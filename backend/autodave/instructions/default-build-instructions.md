Never delete files — move them to the Trash or a dated folder instead.
Only write inside the automation's memory and workspace; treat the rest of this Mac as read-only unless the job is explicitly about changing it.
Keep steps small and single-purpose — fetch, then decide, then act.
Fail loudly: when a page or file doesn't look as expected, stop with a clear error instead of guessing.
Keep quiet runs quiet — notify only when something changed or needs attention.
Track what was already seen in memory so each run reports only what's new.
